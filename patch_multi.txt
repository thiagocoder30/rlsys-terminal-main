                {/* Multi-Session Intelligence Engine (Sprint-037) */}
                {multiSession && (
                    <div className="bg-gray-900 border border-gray-800 rounded-xl p-4 space-y-4 font-mono">
                        <div className="flex items-center justify-between border-b border-gray-800 pb-3">
                            <h2 className="text-xs font-semibold text-gray-300 uppercase tracking-wider flex items-center gap-2">
                                <Network className="w-4 h-4 text-emerald-400" />
                                MULTI-SESSION INTELLIGENCE (MSIE / SPRINT-037)
                            </h2>
                            <span className="text-[10px] text-gray-400 bg-gray-800 px-2 py-0.5 rounded">
                                HISTORICAL CONSOLIDATION
                            </span>
                        </div>

                        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Correlation Index</span>
                                <span className="text-emerald-400 text-lg font-bold">{(multiSession.correlationIndex * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Inst. Stability</span>
                                <span className="text-blue-400 text-lg font-bold">{(multiSession.stabilityIndex * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Seasonality Score</span>
                                <span className="text-purple-400 text-lg font-bold">{(multiSession.seasonalityScore * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Hist. Confidence</span>
                                <span className="text-cyan-400 text-lg font-bold">{(multiSession.institutionalConfidence * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3 col-span-2 md:col-span-4">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Recurring Patterns</span>
                                <div className="flex flex-wrap gap-2 mt-2">
                                    {multiSession.recurringPatterns.map((pattern, idx) => (
                                        <span key={idx} className="text-xs bg-gray-800 text-gray-300 px-2 py-1 rounded">
                                            {pattern}
                                        </span>
                                    ))}
                                    {multiSession.recurringPatterns.length === 0 && (
                                        <span className="text-xs text-gray-500">NO RECURRING PATTERNS DETECTED</span>
                                    )}
                                </div>
                            </div>
                        </div>
                    </div>
                )}
