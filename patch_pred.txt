                {/* Predictive Scenario Simulation Engine (Sprint-036) */}
                {predictive && (
                    <div className="bg-gray-900 border border-gray-800 rounded-xl p-4 space-y-4 font-mono">
                        <div className="flex items-center justify-between border-b border-gray-800 pb-3">
                            <h2 className="text-xs font-semibold text-gray-300 uppercase tracking-wider flex items-center gap-2">
                                <Binoculars className="w-4 h-4 text-emerald-400" />
                                PREDICTIVE SCENARIO SIMULATION ENGINE (PSSE / SPRINT-036)
                            </h2>
                            <span className="text-[10px] text-gray-400 bg-gray-800 px-2 py-0.5 rounded">
                                FORECAST (OBSERVATIONAL)
                            </span>
                        </div>

                        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Dominant Scenario</span>
                                <span className="text-emerald-400 text-lg font-bold">{predictive.dominantScenario}</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Confidence</span>
                                <span className="text-blue-400 text-lg font-bold">{(predictive.confidence * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3 lg:col-span-2">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Probability Distribution</span>
                                <div className="space-y-1 mt-2">
                                    {Object.entries(predictive.probabilityDistribution).map(([scenario, prob]) => (
                                        <div key={scenario} className="flex items-center text-xs">
                                            <span className="w-32 text-gray-400">{scenario}</span>
                                            <div className="flex-1 h-2 bg-gray-800 rounded overflow-hidden">
                                                <div 
                                                    className="h-full bg-purple-500" 
                                                    style={{ width: `${(prob as number) * 100}%` }}
                                                />
                                            </div>
                                            <span className="w-12 text-right text-gray-300">{(prob as number * 100).toFixed(0)}%</span>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}
