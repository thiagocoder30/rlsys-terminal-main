                {/* Institutional Intelligence Evolution Engine (Sprint-035) */}
                {evolution && (
                    <div className="bg-gray-900 border border-gray-800 rounded-xl p-4 space-y-4 font-mono">
                        <div className="flex items-center justify-between border-b border-gray-800 pb-3">
                            <h2 className="text-xs font-semibold text-gray-300 uppercase tracking-wider flex items-center gap-2">
                                <Globe className="w-4 h-4 text-emerald-400" />
                                INSTITUTIONAL INTELLIGENCE EVOLUTION ENGINE (IIEE / SPRINT-035)
                            </h2>
                            <span className="text-[10px] text-gray-400 bg-gray-800 px-2 py-0.5 rounded">
                                MACRO OBSERVATION
                            </span>
                        </div>

                        <div className="grid grid-cols-2 md:grid-cols-6 gap-4">
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Intelligence Index</span>
                                <span className="text-emerald-400 text-lg font-bold">{(evolution.institutionalIntelligenceIndex * 100).toFixed(1)}</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Evolution Score</span>
                                <span className="text-blue-400 text-lg font-bold">{(evolution.operationalEvolutionScore * 100).toFixed(1)}</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Knowledge Growth</span>
                                <span className="text-fuchsia-400 text-lg font-bold">{(evolution.knowledgeGrowth * 100).toFixed(1)}</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Learning Efficiency</span>
                                <span className="text-amber-400 text-lg font-bold">{(evolution.learningEfficiency * 100).toFixed(1)}</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Adaptive Stability</span>
                                <span className="text-cyan-400 text-lg font-bold">{(evolution.adaptiveStability * 100).toFixed(1)}</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Quality Trend</span>
                                <span className="text-purple-400 text-lg font-bold">{(evolution.decisionQualityTrend * 100).toFixed(1)}</span>
                            </div>
                        </div>
                    </div>
                )}
