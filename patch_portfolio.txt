                {/* Portfolio Intelligence Layer (Sprint-038) */}
                {portfolio && (
                    <div className="bg-gray-900 border border-gray-800 rounded-xl p-4 space-y-4 font-mono">
                        <div className="flex items-center justify-between border-b border-gray-800 pb-3">
                            <h2 className="text-xs font-semibold text-gray-300 uppercase tracking-wider flex items-center gap-2">
                                <PieChart className="w-4 h-4 text-emerald-400" />
                                PORTFOLIO INTELLIGENCE LAYER (PIL / SPRINT-038)
                            </h2>
                            <span className="text-[10px] text-gray-400 bg-gray-800 px-2 py-0.5 rounded">
                                SYSTEM LEVEL AGGREGATION
                            </span>
                        </div>

                        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Portfolio Health</span>
                                <span className="text-emerald-400 text-lg font-bold">{(portfolio.portfolioHealth * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Diversification</span>
                                <span className="text-blue-400 text-lg font-bold">{(portfolio.diversificationIndex * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Concentration</span>
                                <span className="text-orange-400 text-lg font-bold">{(portfolio.concentrationIndex * 100).toFixed(1)}%</span>
                            </div>
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Portfolio Confidence</span>
                                <span className="text-cyan-400 text-lg font-bold">{(portfolio.portfolioConfidence * 100).toFixed(1)}%</span>
                            </div>
                            
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3 col-span-2 md:col-span-2">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Strategy Contribution</span>
                                <div className="space-y-1 mt-2">
                                    {portfolio.strategyContributions.map((strat) => (
                                        <div key={strat.strategyId} className="flex items-center text-xs">
                                            <span className="w-24 text-gray-400 truncate">{strat.strategyId}</span>
                                            <div className="flex-1 h-2 bg-gray-800 rounded overflow-hidden">
                                                <div 
                                                    className="h-full bg-emerald-500" 
                                                    style={{ width: `${(strat.contributionScore) * 100}%` }}
                                                />
                                            </div>
                                            <span className="w-12 text-right text-gray-300">{(strat.contributionScore * 100).toFixed(0)}%</span>
                                        </div>
                                    ))}
                                </div>
                            </div>
                            
                            <div className="bg-black/60 border border-gray-800 rounded-lg p-3 col-span-2 md:col-span-2">
                                <span className="text-gray-500 text-[10px] uppercase block mb-1">Regime Exposure</span>
                                <div className="space-y-1 mt-2">
                                    {portfolio.regimeExposure.map((regime) => (
                                        <div key={regime.regime} className="flex items-center text-xs">
                                            <span className="w-24 text-gray-400 truncate">{regime.regime}</span>
                                            <div className="flex-1 h-2 bg-gray-800 rounded overflow-hidden">
                                                <div 
                                                    className="h-full bg-purple-500" 
                                                    style={{ width: `${(regime.exposurePercentage) * 100}%` }}
                                                />
                                            </div>
                                            <span className="w-12 text-right text-gray-300">{(regime.exposurePercentage * 100).toFixed(0)}%</span>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}
